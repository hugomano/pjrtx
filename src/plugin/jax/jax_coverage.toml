# PjRTx-owned JAX completion scoreboard.

[suites.local_smoke]
target = "//src/plugin/jax:jax_pytest_smoke_test"
status = "supported"
scope = "pytest-shaped local plugin registration, CPU comparison, trace checks"

[suites.current_op_suite]
target = "//src/plugin/jax:jax_op_suite_test"
status = "supported"
scope = "broad supported StableHLO op forms used by JAX/ZML"

[suites.custom_call]
target = "//src/plugin/jax:jax_custom_call_test"
status = "supported"
scope = "JAX FFI custom call lowering into backend-native MLX custom call registry"

[suites.rng_u64]
target = "//src/plugin/jax:jax_rng_u64_test"
status = "supported"
scope = "x64 Threefry rng_bit_generator parity with CPU"

[suites.llama_like]
target = "//src/plugin/jax:jax_llama_like_inference_test"
status = "supported"
scope = "resident constants, repeated execute, no fallback, activation-only H2D"

[suites.upstream_subset]
target = "//src/plugin/jax:jax_upstream_subset_test"
status = "manual"
scope = "allowlist-driven upstream JAX pytest nodes from pinned official @jax_upstream"

[xfail.gspmd]
reason = "PjRTx intentionally does not implement GSPMD in this phase."

[xfail.multi_host]
reason = "Cross-host rendezvous and distributed process behavior are out of scope for the Metal/MLX bootstrap."
